# Jack 書局業務網站 - Astro + React + Framer Motion

這是一個使用 Astro、React 和 Framer Motion 建立的現代化書局業務個人網站。

## 🚀 技術棧

- **Astro** - 靜態網站生成器
- **React** - UI 組件框架
- **Framer Motion** - 動畫庫
- **Tailwind CSS** - CSS 框架
- **TypeScript** - 類型安全

## 📋 前置需求

在開始之前，請確保您的系統已安裝：

- **Node.js** (版本 18.14.1 或更高)
- **npm** (通常隨 Node.js 一起安裝)

### 安裝 Node.js

如果您還沒有安裝 Node.js，請前往 [Node.js 官網](https://nodejs.org/) 下載並安裝 LTS 版本。

安裝完成後，在命令提示字元中執行以下命令驗證安裝：

```bash
node --version
npm --version
```

## 🛠️ 安裝步驟

1. **安裝依賴**

```bash
npm install
```

2. **啟動開發伺服器**

```bash
npm run dev
```

開發伺服器將在 `http://localhost:4321` 啟動。

3. **建置生產版本**

```bash
npm run build
```

建置完成的檔案將位於 `dist/` 目錄中。

4. **預覽生產版本**

```bash
npm run preview
```

## 📁 專案結構

```
JACKMODE/
├── public/              # 靜態資源
│   └── favicon.svg      # 網站圖示
├── src/
│   ├── components/      # React 組件
│   │   ├── Navigation.tsx
│   │   ├── Hero.tsx
│   │   ├── Services.tsx
│   │   ├── About.tsx
│   │   ├── Recommendations.tsx
│   │   ├── Contact.tsx
│   │   ├── Footer.tsx
│   │   └── Toast.tsx
│   ├── layouts/         # Astro 佈局
│   │   └── Layout.astro
│   ├── pages/           # 頁面
│   │   └── index.astro
│   └── styles/          # 全域樣式
│       └── global.css
├── astro.config.mjs     # Astro 配置
├── tailwind.config.mjs  # Tailwind 配置
├── tsconfig.json        # TypeScript 配置
└── package.json         # 專案依賴
```

## ✨ 功能特色

### 動畫效果

- **導航欄**: 滾動時背景變化，手機選單滑動動畫
- **Hero 區塊**: 淡入動畫
- **服務卡片**: 滾動觸發的交錯動畫，Hover 放大效果
- **關於區塊**: 滾動觸發的左右滑入動畫
- **推薦書籍**: 交錯動畫，Hover 上浮效果
- **聯絡區塊**: 淡入動畫

### 互動功能

- **響應式設計**: 完美支援桌面、平板和手機
- **手機選單**: 漢堡選單與滑動動畫
- **LINE ID 複製**: 一鍵複製 LINE ID 並顯示提示
- **表單提交**: 聯絡表單（需要後端整合）
- **平滑滾動**: 錨點連結平滑滾動

## 🎨 自訂設計

### 顏色配置

在 `tailwind.config.mjs` 中定義了自訂顏色：

- `primary`: #5D4037 (深咖啡色)
- `secondary`: #D7CCC8 (淺棕色)
- `accent`: #E65100 (活力橘)
- `paper`: #FDFBF7 (米白色)
- `ink`: #263238 (深灰藍)

### 字體

- **Sans-serif**: Noto Sans TC
- **Serif**: Noto Serif TC

## 📝 自訂內容

### 修改聯絡資訊

在以下組件中更新您的聯絡資訊：

- `src/components/Contact.tsx` - 電話、EMAIL、LINE ID
- `src/components/Hero.tsx` - LINE ID 複製功能

### 修改推薦書籍

編輯 `src/components/Recommendations.tsx` 中的 `books` 陣列。

### 修改服務項目

編輯 `src/components/Services.tsx` 中的 `services` 陣列。

## 🚀 部署

### Vercel

1. 將專案推送到 GitHub
2. 在 [Vercel](https://vercel.com) 導入專案
3. Vercel 會自動偵測 Astro 並進行部署

### Netlify

1. 將專案推送到 GitHub
2. 在 [Netlify](https://netlify.com) 導入專案
3. 建置命令: `npm run build`
4. 發布目錄: `dist`

## 📄 授權

此專案僅供個人使用。

## 🙋 支援

如有任何問題，請聯絡 Jack：
- LINE ID: jack_book888
- Email: jack@bookstore.com.tw
- 電話: 0912-345-678
